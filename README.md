
  # Recreate Design in Figma

  This is a code bundle for Recreate Design in Figma. The original project is available at https://www.figma.com/design/7WISimQZYUM64AtYV9vOuU/Recreate-Design-in-Figma.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  